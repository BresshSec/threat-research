# Indicators of Compromise — Case Study 003

These indicators are drawn from the **primary sandbox evidence set** and are provided for defensive use.

| Type | Indicator | Evidence context |
|---|---|---|
| Domain | `danielenittisotres[.]it` | Deep-dive analyzed website; treat as potentially compromised/victim infrastructure |
| Domain | `fingerprint-veri[.]info` | Remote component observed in sandbox HTTP activity |
| Domain | `mioiooo[.]icu` | Observed in sandbox IOC set |
| Domain | `clickzona[.]net` | Observed in sandbox IOC set |
| Domain | `cloudflare-check[.]net` | Observed in sandbox IOC set |
| Domain | `norediam[.]com` | Observed in sandbox IOC set |
| Domain | `triapfog[.]com` | Observed in sandbox IOC set |
| IP | `89.46.106[.]61` | Observed sandbox IOC |
| IP | `58.64.137[.]69` | Observed sandbox IOC |
| IP | `178.16.52[.]101` | Observed sandbox IOC |

## Interpretation

An indicator appearing here does not automatically mean its owner/operator is malicious. Legitimate sites may be compromised and infrastructure may be shared or abused.

Indicators can age quickly. Validate current relevance before blocking.

**Naming note:** the primary evidence contains `fingerprint-veri[.]info`. The similarly named `fingerprint-verification[.]info` is not substituted for it in this evidence set.
